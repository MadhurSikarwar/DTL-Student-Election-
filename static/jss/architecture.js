// Ensure SVG matches container size on resize
window.addEventListener('resize', () => {
    const svg = document.querySelector('svg.connections');
    // Logic to redraw or adjust if needed, mostly handled by CSS percentage width/height
});
